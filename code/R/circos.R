
# Load package
install.packages("RCircos")
library(RCircos)

#---------------------------------------------------------------------------------------------------#

# Define files and variables
genome_file <- ''
transcripts_file1 <- ''
transcripts_file2 <- ''
transcripts_file3 <- ''
transcripts_file4 <- ''

#---------------------------------------------------------------------------------------------------#

# Read in data
genome <- read.delim(genome_file, sep='\t', header=FALSE, row.names=1)
transcripts1 <- read.delim(transcripts_file1, sep='\t', header=FALSE, row.names=1)
transcripts2 <- read.delim(transcripts_file2, sep='\t', header=FALSE, row.names=1)
transcripts3 <- read.delim(transcripts_file3, sep='\t', header=FALSE, row.names=1)
transcripts4 <- read.delim(transcripts_file4, sep='\t', header=FALSE, row.names=1)

#---------------------------------------------------------------------------------------------------#

# Format data







#---------------------------------------------------------------------------------------------------#

# Generate plot





